using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Util 
{
    public static T FindChild<T>(GameObject go, string name = null, bool recursive = false) where T : UniyEngine.Object
    {
        if (go == null)
            return null;

        if (recursive == false)
        {

        }
        else
        {

        }
        return null;
    }
}
